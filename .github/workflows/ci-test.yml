on: 
  push:
    branches:
      - '**'
jobs:
  install-sprkl-test:
    runs-on: ubuntu-latest
    name: Installing Sprkl Testing
    steps:

      - uses: actions/checkout@v3


      - name: Sprkl Setup and Post Push
        uses: sprkl-dev/sprkl-action/post-run-push@13fbd81808605bf19cceb6a0d395550f10334229
        with:
          npm_token: ${{ secrets.SPRKL_NPM_TOKEN }}
          setenv: false
          token: ${{ secrets.SPRKL_CI_TOKEN }}

      - run: yarn install
      - run: yarn test:e2e:sprkl

      
